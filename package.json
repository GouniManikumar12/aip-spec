{
  "name": "amxp-spec",
  "version": "0.1.0",
  "description": "Reference specification, schemas, and fixtures for the AMXP protocol.",
  "license": "Apache-2.0",
  "private": true,
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "lint": "echo \"No linting configured for the spec workspace yet.\"",
    "test": "node -e \"console.log('Conformance tests run via external tooling.');\""
  },
  "keywords": [
    "amxp",
    "specification",
    "schema",
    "conformance"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/admesh/amxp-spec"
  }
}
